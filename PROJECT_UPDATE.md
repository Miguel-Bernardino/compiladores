Project update since first delivery

Summary of changes made for Etapa 6 (LEX):
- Implemented lexical scanner in `LexerScanner/LexerScanner.py`.
- Adjusted level-2 repairs for real constants and strings.

Files touched (high level):
- LexerScanner/LexerScanner.py (main changes and comment translations)
- Various reserved/token/symbol modules (no functional change unless noted)

How to validate
1. From project root run: `python main.py MeuTeste`
2. Confirm `MeuTeste.LEX` and `MeuTeste.TAB` are generated and contain expected entries.

If you want a detailed changelog (git-style diff) or a packaged archive of the source, tell me and I will prepare it.
